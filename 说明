---模拟的云盘

---无后端

---模拟数据的介绍：采用单层对象数组，通过id与pid来关联数据的父子级关系
例如{id：0，
    pid:-1
    }
    {id:1,
    pid:0}
    id为0的数据为id:1的父级数据
    实现数据的层级渲染
    
---模拟数据可以进行的操作：
            移动，删除，新增，重命名，右键菜单，勾选后的移动和删除功能
